import 'package:flutter/material.dart';

const sampleAppOutterPadding = EdgeInsets.only(top: 16, right: 24, left: 24);
const sccOutterPadding = EdgeInsets.only(top: 16, right: 25, left: 25);
const sccOutterMargin = EdgeInsets.only(left: 17, right: 17, top: 16);

const sccMargin = EdgeInsets.only(left: 17, right: 17, top: 16);

const sccMarginHorizontal = EdgeInsets.only(left: 17, right: 17);

const sccMarginBtn = EdgeInsets.only(top: 12, bottom: 12);

const sccDashboardPadding = EdgeInsets.only(top: 16, bottom: 16);

const sccInnerPadding =
    EdgeInsets.only(top: 16, right: 12, left: 12, bottom: 16);

const sccSearchFieldPadding = EdgeInsets.only(top: 16, right: 17, left: 17);

const sccParagraph = EdgeInsets.only(left: 8);

const sccBoxedParagraph = EdgeInsets.only(left: 8, top: 16, bottom: 16);

const sccDialogPadding =
    EdgeInsets.only(top: 30, right: 24, left: 24, bottom: 20);
